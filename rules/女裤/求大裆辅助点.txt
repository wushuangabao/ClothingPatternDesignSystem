规则 求大裆辅助点
输入 点 c
输入 点 f
输入 点 g

参数 gx = g.横坐标
参数 cx = c.横坐标
参数 cy = c.纵坐标
参数 hx = (gx + cx)/2
点 h = (hx, cy)
直线 fh = f 连接 h

点 k = 求垂足(c, fh)
点 l = 等分点(c, k, 1/3)
点 v = (0, -1)
l = 求偏移(l, 0.4, v)

输出 l